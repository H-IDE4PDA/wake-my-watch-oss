<!-- Matches the dark navy border baked into the artwork, so the mask edge is invisible. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="#0A1A2E" />
</shape>
