package com.h_ide4pda.wakemywatch.watch.ui

import androidx.compose.ui.graphics.Color

/**
 * Single source of truth for watch UI colors. Change the palette here — every screen
 * that references these follows automatically, including on a future design-mockup pass.
 */
object WatchColors {
    val Background = Color.Black
    val Card = Color(0xFF081526)
    val Purple = Color(0xFF7B61F6)
    val Blue = Color(0xFF00C2FF)
    val Green = Color(0xFF00E676)
    val Muted = Color(0xFFA9B4C8)
    val Text = Color.White
}
